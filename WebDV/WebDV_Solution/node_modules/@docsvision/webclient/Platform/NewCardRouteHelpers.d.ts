/** Формирует адрес для создания карточки. Значение может быть передано в метод goTo сервиса {@link $Router}. */
export declare function getCardCreateRoute(cardTypeId: string, cardKindId?: string): string;
