/** Формирует адрес для открытия строки на редактирование. Значение может быть передано в метод goTo сервиса {@link $Router}. */
export declare function getRowEditRoute(cardId: string, sectionId: string, rowId: string): string;
/** Формирует адрес для открытия строки на просмотр. Значение может быть передано в метод goTo сервиса {@link $Router}. */
export declare function getRowViewRoute(cardId: string, sectionId: string, rowId: string): string;
