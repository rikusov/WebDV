import { IFileDownload } from '@docsvision/webclient/Platform/$FileDownload';
import { $RequestManager } from '@docsvision/webclient/System/$RequestManager';
import "file-saver";
/** Сервис для доступа к функциям объекта window.location */
export declare class FileDownloadService implements IFileDownload {
    private services;
    constructor(services: $RequestManager);
    download(url: string): void;
    saveAs(url: string, fileName: string): JQueryDeferred<Blob>;
}
