/** Формирует адрес для открытия карточки на редактирование. Значение может быть передано в метод goTo сервиса {@link $Router}. */
export declare function getCardEditRoute(cardId: string): string;
/** Формирует адрес для открытия карточки на просмотр. Значение может быть передано в метод goTo сервиса {@link $Router}. */
export declare function getCardViewRoute(cardId: string): string;
