import { Panel, PanelParams } from "@docsvision/webclient/Platform/Panel";
import { RowContainerImpl, RowContainerState } from "@docsvision/webclient/Platform/RowContainerImpl";
/**
 * Содержит публичные свойства элемента управления [Контейнер строка]{@link RowContainer}.
 */
export declare class RowContainerParams extends PanelParams {
    /** Стандартный CSS класс со стилями элемента управления */
    standardCssClass?: string;
    /** Строка, разделяющая значения дочерних элементов управления. По умолчанию не задан. */
    delimiter?: string;
    /** Метка строки. */
    labelText?: string;
    services?: any;
}
/**
 * Класс элемента управления "Контейнер строка".
 *
 * Добавляет в web-разметку сворачиваемый элемент управления с заголовком, предназначенный для встраивания других элементов управления.
 */
export declare class RowContainer extends Panel<RowContainerParams, RowContainerState> {
    protected createParams(): RowContainerParams;
    protected createImpl(): RowContainerImpl;
}
