/** Формирует адрес для создания строки. Значение может быть передано в метод goTo сервиса {@link $Router}. */
export declare function getRowCreateRoute(cardId: string, sectionId: string, parentRowId?: string, parentTreeRowId?: string): string;
