export declare class PageLeaveConfirmation {
    condition: () => boolean;
    message: string;
    constructor(condition?: () => boolean, message?: string);
    static Setup(condition?: () => boolean): PageLeaveConfirmation;
    dispose(): void;
    private onBeforeWindowUnload;
}
