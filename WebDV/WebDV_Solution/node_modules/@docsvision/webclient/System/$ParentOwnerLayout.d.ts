import { ILayout } from './$Layout';
export declare type $ParentOwnerLayout = {
    parentOwnerLayout: ILayout;
};
export declare const $ParentOwnerLayout: string | ((model?: $ParentOwnerLayout) => ILayout);
