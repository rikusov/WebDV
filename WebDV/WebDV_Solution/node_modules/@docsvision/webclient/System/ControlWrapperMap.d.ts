import { LayoutControl } from "@docsvision/webclient/System/BaseControl";
/** Словарь контролов с доступом по имени. */
export declare class ControlWrapperMap {
    [name: string]: any;
    /** Возвращает элемент управления с заданным именем и выводит в консоль сообщение об ошибке, если элемент управления не найден. */
    get<T extends LayoutControl | LayoutControl[]>(name: string): T;
    /** Возвращает элемент управления с заданным именем */
    tryGet<T extends LayoutControl | LayoutControl[]>(name: string): T;
    getRoot: () => LayoutControl;
}
