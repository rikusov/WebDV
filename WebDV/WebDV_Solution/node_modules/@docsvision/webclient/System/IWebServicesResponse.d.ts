export interface IWebServicesResponse<T> {
    /** Выполнился ли запрос успешно. */
    success: boolean;
    /** Сообщение об ошибке, в случае если {@link success} = false. */
    errorMessage?: string;
    /** JavaScript объект, содержащий результаты выполнения запроса */
    data?: T;
}
