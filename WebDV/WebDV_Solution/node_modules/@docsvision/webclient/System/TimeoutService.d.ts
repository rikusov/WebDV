import { ITimeoutService } from '@docsvision/webclient/System/$Timeout';
export declare class TimeoutService implements ITimeoutService {
    waitTimeout(ms: number): JQueryDeferred<void>;
    setTimeout(callback: Function, ms: number): any;
    setInterval(handler: TimerHandler, timeout?: number): any;
}
