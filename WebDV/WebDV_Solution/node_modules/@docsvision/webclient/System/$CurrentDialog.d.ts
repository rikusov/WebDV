export interface IDialog {
    close(): any;
}
export declare type $CurrentDialog = {
    currentDialog: IDialog;
};
export declare const $CurrentDialog: string | ((model?: $CurrentDialog) => IDialog);
