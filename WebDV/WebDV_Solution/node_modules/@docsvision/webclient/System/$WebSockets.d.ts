export interface IWebSocketsService {
    create(url: string, protocols?: string | string[]): WebSocket;
}
export declare type $WebSockets = {
    webSockets: IWebSocketsService;
};
export declare const $WebSockets: string | ((model?: $WebSockets) => IWebSocketsService);
